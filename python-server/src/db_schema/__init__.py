"""SQL schema files for the document database.

This package exists so that the .sql files under it can be located via
importlib.resources, regardless of how the python-server is installed
or invoked.
"""
